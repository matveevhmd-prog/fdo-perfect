# ARCHI — план від 13.09.2026 до релізу

Звірено з живим кодом (n8n workflow `CKwCsGIwyw9feCY5`, versionId
`85cc4954-a4ff-45be-8912-fb35cd23b7c6`), не тільки з документами ТЗ.
Документ ТЗ v2.0 (05.09.2026) в кількох місцях відстає від реального коду
(в плюс — частина "не реалізовано" вже зроблена) і в одному місці код
випередив документ без вашого явного підтвердження, поки не отриманого
цією сесією (13.09.2026, Meeting_Log-формат — тепер підтверджено).

## Стан на 13.09.2026 (перевірено кодом)

| Крок ТЗ (Розділ 11) | Статус | Доказ |
|---|---|---|
| 1. Видалити мертві WR:Protocol-ноди | ✅ готово | відсутні в 128 нодах workflow |
| 2. Гейт "фінал" для Time_Log/Money_Log | ✅ готово | `Import: Parse Agent Output` форсує DRAFT, якщо `execution_mode !== 'generate_import_final'`; валідація рядків (`cleanTimeRows`/`cleanMoneyRows`) перед insert у `public.time_log`/`public.money_log` |
| 3. Meeting_Log | ✅ готово, **підтверджено власником 13.09** | таблиця `public.meeting_log`, команда `зустріч DEMO-XX`, той самий жорсткий гейт |
| 4. ETC_прогноз по 4 стадіях окремо | ✅ готово | `wr_weekly_snapshots.залишок_прогноз_по_стадіях` (jsonb), парситься в `WR: Parse Agent Output` |
| 5. Обов'язковий брифінг по `wr_plan_items` | ✅ готово | carryover-логіка + `WR: Send Project Confirmation` жорстко підключений до `WR: Format Plan For Project Confirm`, не через LLM |
| 6. Потік Г (Щотижневий рендер) | ✅ готово, з дефектом безпеки | читає `project_metrics`+`wr_weekly_snapshots` з Postgres; формула Готовність_прогноз збігається з Розділом 6 буквально; грошових сум немає — **але Data Webhook не авторизований, див. SECURITY.md п.2** |
| 7. Потік Д (.xlsx експорт) | ❌ не розпочато | жодної відповідної ноди немає |
| 8. Крок Б3 (авто-розрахунок прогноз-блоку `project_metrics` на "фінал") | ❌ не розпочато | існує лише `Sync Flat To Metrics Webhook` — це "Ручний інжект EVM-даних" (гілка 3 з ТЗ, історична/демо, не production), копіює вже пораховані числа з замороженого Flat, не рахує їх сам |
| 9. Mini App + Chat-агент → `project_metrics` | ❌ не розпочато, залежить від 8 | `Build MiniApp Data` ← `Read Weekly Log`; `Build Request Body` (Chat) ← `Read DEMO Flat (Chat)` — обидва 100% на Google Sheets, жодного Postgres-запиту в цьому шляху |

## Нове, знайдене цією сесією (не було в ТЗ)

- 🔴 **Weekly Render Data Webhook без InitData-перевірки** — регресія,
  внесена при побудові Потоку Г. Деталі й фікс — `SECURITY.md`.
- ⚠️ Активний, не задокументований у жодному з трьох текстових джерел
  workflow `Archi Representative Telegram Bot` (`I3K5tBj0FWF1dbWb`) —
  за рішенням власника, окремий проєкт, не чіпати в межах ARCHI.
- `real_rate_team_hour` (Розділ 12 ТЗ, відкрите питання №3) — досі не
  вирішено: код просто пропускає значення, не інтерпретуючи, яка це ставка
  (A5 проектна чи 153грн/год корпоративна).
- Формат `stage_data` (jsonb) де-факто вже визначений кодом
  `Sync: Build Update Rows`: `{"МБР":{readiness_plan, readiness_forecast,
  etc_plan_hours, etc_forecast_hours}, ...}` — але тільки в ручному
  Sync-скрипті, не в автоматичному Кроці 8, якого ще немає.

## План

### Фаза 0 (13-15.09) — без цього далі не рухатись
1. Пофіксити InitData-гейт на `Weekly Render Data Webhook` (підключити
   наявні, але відключені `WR-Render: Validate InitData`/`Is Valid
   InitData`) — години, не дні.
2. Рішення власника по `real_rate_team_hour` і по остаточності формату
   `stage_data`.
3. Токен `@archi_demo_bot` — після сьогоднішнього публічного git-push
   ротація перестає бути "після демо" опцією, а стає передумовою будь-якого
   виходу за межі внутрішнього тестування. Рішення власника — коли саме.

### Фаза 1 (15-19.09) — Крок 8, Б3
Побудувати автоматичний розрахунок прогноз-блоку `project_metrics`, що
спрацьовує в момент `tag=FINAL_REPORT` у WR-потоці (не через ручний
Sync-webhook), рахуючи з `wr_weekly_snapshots` + `time_log`/`money_log` за
формулами Розділу 6 ТЗ. Тест на DEMO-01, звірка з контрольним числом.

### Фаза 2 (22-26.09) — Крок 9
Перевести `Build MiniApp Data` і `Build Request Body` (Chat) на
`project_metrics` замість Flat. Регресійний ризик для вже робочих фіч —
тестувати на всіх 10 проєктах, не тільки DEMO-01.

### Фаза 3 (29.09-03.10) — Крок 7 + безпека
Експорт `.xlsx` (Потік Д); ротація токена бота (якщо не зроблено в Фазі 0);
повний наскрізний прогін тижневого циклу по всіх 10 демо-проєктах.

### Фаза 4 (06-10.10) — буфер
Фікс знайденого, підготовка до першого реального (не DEMO) клієнта.

**Орієнтовний реліз: 10-13.10.2026** [Припущення — дедлайну немає в жодному
документі, оцінка від обсягу залишкової роботи, за умови що Фаза 0
закриється цього тижня].
